# 语言特征与攻击成功率 (ASR) 回归分析方案

## 一、研究背景

本项目旨在系统性识别哪些语言特征使越狱攻击更容易成功，基于论文 "Low-Resource Languages Jailbreak GPT-4" 的思路。

**目标**：
1. 识别"高漏洞语言特征组合"
2. 为构建"最具威胁性语言子集"提供依据

---

## 二、数据准备

### 2.1 数据源

| 数据源 | 文件路径 | 内容 |
|--------|---------|------|
| 语言特征 | `config/languages.yaml` | syntax_distance_weighted, training_data_estimate, morph_type, basic_word_order, resource_level, family |
| ASR 数据 | `outputs/llm/deepseek-chat/advbench_output_deepseek-chat.csv` | evaluation 结果，需按语言分组统计 |
| 距离度量 | `data/processed/final_distance_metrics.csv` | gower_distance, hamming_distance |

### 2.2 语言列表（12种）

| 代码 | 语言 | 资源级别 | 形态类型 | 词序 |
|-----|------|---------|---------|------|
| zu | 祖鲁语 | LRL | Agglutinative | SVO |
| gd | 苏格兰盖尔语 | LRL | Fusional | VSO |
| hmn | 苗语 | LRL | Isolating | SVO |
| gn | 瓜拉尼语 | LRL | Agglutinative | SVO |
| uk | 乌克兰语 | MRL | Fusional | SVO |
| bn | 孟加拉语 | MRL | Fusional | SOV |
| th | 泰语 | MRL | Isolating | SVO |
| he | 希伯来语 | MRL | Fusional | SVO |
| zh-CN | 简体中文 | HRL | Isolating | SVO |
| ar | 阿拉伯语 | HRL | Fusional | VSO |
| it | 意大利语 | HRL | Fusional | SVO |
| hi | 印地语 | HRL | Fusional | SOV |

---

## 三、特征工程

### 3.1 特征编码方案

| 特征 | 类型 | 编码方式 |
|------|------|---------|
| `syntax_distance_weighted` | 数值 | 保持原样（0-1） |
| `training_data_estimate` | 数值 | 对数转换 log(x+ε) |
| `gower_distance` | 数值 | 保持原样 |
| `morph_type` | 分类 | 独热编码 → 2 维 |
| `basic_word_order` | 分类 | 独热编码 → 2 维 |
| `resource_level` | 有序 | 序数编码 LRL=0, MRL=1, HRL=2 |
| `family` | 分类 | 合并为 Indo-European vs Other |

### 3.2 交互特征
- `resource_level × syntax_distance_weighted`
- `morph_type_Isolating × resource_level`

---

## 四、回归模型选择（N=12 小样本）

考虑到只有 12 个样本点，采用多方法交叉验证：

| 模型 | 用途 | 库 |
|------|------|-----|
| **Spearman 秩相关** | 单变量分析 | scipy |
| **贝叶斯线性回归** | 主模型，量化不确定性 | PyMC (简化版) |
| **LASSO 回归** | 特征选择 | scikit-learn |
| **随机森林** | 非线性探索，特征重要性 | scikit-learn |

### 4.1 验证策略
- 留一法交叉验证 (LOOCV)
- Bootstrap 重采样（1000 次）评估稳定性

---

## 五、特征重要性分析

采用多方法整合评分：

1. **贝叶斯**：后验概率 P(β≠0|data)
2. **LASSO**：非零标准化系数
3. **随机森林**：排列重要性 + MDI
4. **Spearman**：相关系数绝对值

**最终重要性** = 0.3×贝叶斯 + 0.3×LASSO + 0.2×RF + 0.2×Spearman

---

## 六、高漏洞语言特征组合识别

### 6.1 定义
- 高漏洞：ASR > 中位数

### 6.2 识别方法
1. **决策树**：max_depth=2-3，寻找分割规则
2. **关联规则**：Apriori 算法（最小支持度=0.3，置信度=0.7）
3. **子群发现**：寻找 ASR 显著高于平均的特征子集
4. **领域知识候选组合**：
   - 孤立语 + 低资源
   - 低资源 + 高语法距离
   - 非印欧语系 + 低训练数据

### 6.3 组合评分
- 覆盖度：符合组合的语言比例
- 提升度：组合内 ASR / 总体 ASR
- 纯度：组合内高漏洞语言比例

---

## 七、使用方法

### 步骤 1：完成实验数据收集
```bash
# 运行 100 条指令的完整测试
python scripts/extract_advbench_output.py --limit 100
```

### 步骤 2：运行完整分析
```bash
python scripts/analysis/main_analysis.py
```

### 步骤 3：查看结果
- 分析报告：`outputs/analysis/report.md`
- 可视化图表：`outputs/analysis/figures/`

---

## 八、分析脚本结构

```
scripts/analysis/
├── __init__.py
├── data_preparation.py      # 数据加载、ASR 计算、合并
├── feature_engineering.py   # 特征编码与转换
├── regression_models.py     # 贝叶斯/LASSO/RF 模型
├── feature_importance.py    # 多方法重要性整合
├── pattern_mining.py        # 高漏洞组合识别
├── visualization.py         # 所有可视化
└── main_analysis.py         # 主流程入口
```

---

## 九、可视化方案

### 9.1 探索性
- 相关性热图（Spearman）
- 配对图矩阵（数值特征）
- 箱线图（分类特征 vs ASR）

### 9.2 模型结果
- 贝叶斯系数后验分布森林图
- 特征重要性对比条形图
- 部分依赖图（关键特征）

### 9.3 高漏洞组合
- 决策树可视化
- PCA 2D 投影（ASR 颜色编码）

---

## 十、小样本统计注意事项

1. **限制特征数**：单个模型特征 ≤ 3 个，避免过拟合
2. **强调不确定性**：报告置信区间而非点估计
3. **探索性定位**：结果为假设生成，非验证性结论
4. **效应量优先**：关注效应大小而非 p 值
5. **建议外部验证**：收集更多语言或跨模型验证
